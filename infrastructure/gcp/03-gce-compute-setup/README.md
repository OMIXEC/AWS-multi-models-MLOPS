# Google Compute Engine Setup

This directory is intended for automating the creation of Virtual Machine instances on GCP to host model training or host the Streamlit server for internal testing.

Tools generally used:
* Google Cloud Console
* `gcloud compute instances create ...`
* `google-cloud-compute` Python SDK
