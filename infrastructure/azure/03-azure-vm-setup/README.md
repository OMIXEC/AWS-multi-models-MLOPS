# Azure Virtual Machine Setup

This directory focuses on automating the creation of Azure VMs for ML model training, evaluation, and application hosting.

Tools generally used:
* Azure Portal
* Azure CLI (`az vm create ...`)
* `azure-mgmt-compute` Python SDK
